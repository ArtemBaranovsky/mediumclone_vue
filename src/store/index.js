// import Vue from 'vue'   // not used in Vue 3 init
// import Vuex from 'vuex' // not used in Vue 3 init
import {createStore} from 'vuex'

// Vue.use(Vuex)           // not used in Vue 3 init

// export default new Vuex.Store({    // not used in Vue 3 init
export default new createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
